# JsMock-Poll

Quick project to make sure I understand the different 'this' methods. bind is used to make sure the 'this' target it set to the poll object. call is used to set 'this' to a new array. All answers are displayed in the console.

live preview: https://ecurry15.github.io/JsMock-Poll/